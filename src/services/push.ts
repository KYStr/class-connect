import { notImplemented } from './_stub';

// Web Push (DEVELOPMENT.md §11).
export async function savePushSubscription(_sub: PushSubscriptionJSON): Promise<void> {
  return notImplemented('savePushSubscription');
}
