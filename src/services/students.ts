import type { Student } from '@/types/domain';

// DEVELOPMENT.md §8.2
export async function getRoster(_classId: string): Promise<Student[]> {
  return [];
}

export async function getMyChildren(): Promise<Student[]> {
  return [];
}
