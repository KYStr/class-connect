-- seed.sql — applied by `supabase db reset` (AGENTS.md rail 9).
-- Seeding auth users + classes/students/guardianships requires the auth schema and is added
-- in P1 alongside the account flow (DEVELOPMENT.md §7). Mirror the demo default data
-- (demo/index.html DEFAULT: 一年甲班, 28 students, seat 07 = 小宇) so e2e flows have real content.

-- placeholder: no-op until P1 wires the account/seed flow.
select 1;
